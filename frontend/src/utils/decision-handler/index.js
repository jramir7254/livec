import { API } from '@api/client.js'
import { logger } from '@utils/logger'

const log = logger.create('decisino=handler.js');


export const reject = async (suggestionId, reason) => {
    try {
        
    } catch (error) {
        
    }
}

